// import { flexsearchHelper } from '..//helpers/flexsearch';

// Text search input
export function initSearch() {
  // const searchInput = document.querySelector('#searchInput');
  // const searchPage = document.querySelector('#searchPage');
  // console.assert(searchInput != null);
  // console.assert(searchPage != null);
  // searchInput.addEventListener('click', () => modal.show(searchPage));
  // Data file
  // const section = searchInput.getAttribute('data-section');
  // const filename = `/${section}/index.json`;
  // Events
  //   searchInput.addEventListener('focus', _onFocusSearchInput);
  //   searchInput.addEventListener('blur', _onBlurSearchInput);
  // searchInput.addEventListener('input', _onInputSearchInput);
  // // Results to HTML
  // textSearch.setData(filename);
  // _items = [];
  // _searchContent = document.querySelector(searchContentID);
  //   util.show(searchInputParentID);
  // } else {
  //   util.hide(searchInputParentID);
  // document
  //   .querySelector('.search form')
  //   .addEventListener('click', () => console.warn('show search modal'));
}
